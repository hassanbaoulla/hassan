const TVA = 20.0;
const prixHT = parseInt(prompt('170 ?'));
const reduction = ('benificiez vous d\ une  remise');
if (reduction === oui || reduction === oui)
const remis = parseFloat(prompt('20'));
montant de remis =(prix HT * remis) / 100;
prix HT = prix HT - montantRemis;
const montantTVA +(prixHT * TVA)\ 100;
const prixTTC = (prixHT + montantTVA);
document.write('prixTTC' + prixTTC);

